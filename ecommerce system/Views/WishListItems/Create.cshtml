@model ecommerce_system.Models.WishListItem

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>WishListItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WishListId" class="control-label"></label>
                <select asp-for="WishListId" class ="form-control" asp-items="ViewBag.WishListId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ProudectId" class="control-label"></label>
                <select asp-for="ProudectId" class ="form-control" asp-items="ViewBag.ProudectId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
